{
  "name": "fractal-vue-vite",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"vite --host\" \"NODE_ENV=development node fractal.js --server\"",
    "build": "vite build && NODE_ENV=production node fractal.js --build"
  },
  "devDependencies": {
    "@frctl/fractal": "latest",
    "@frctl/nunjucks": "^2.0.15",
    "@vitejs/plugin-vue": "^5.0.0",
    "concurrently": "^8.2.0",
    "vite": "^5.0.0",
    "vue": "^3.4.0"
  }
}
